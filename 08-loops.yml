- name: loopes using in ansible
  hosts: server
  become: yes
  #connection: local
  tasks:
  - name: print names
    ansible.builtin.package:
      #msg: "installing package"
      name: "{{item}}"
      state: present
    loop:
    - nginx
    - mysql
    - postfix  